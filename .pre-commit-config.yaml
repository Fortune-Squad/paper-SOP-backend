repos:
  - repo: local
    hooks:
      - id: api-doc-check
        name: API documentation sync check
        entry: python scripts/check_api_docs.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
